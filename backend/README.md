# PlanAI Backend API

## Описание

Backend API для веб-приложения PlanAI - системы автоматизированной перепланировки квартир с использованием искусственного интеллекта.

## Основной функционал

### 🔐 Аутентификация
- Регистрация и авторизация пользователей (по номеру телефона)
- Авторизация администраторов (по email)
- JWT токены для защиты API

### 📐 Управление планами
- Загрузка планов квартир (изображения в формате PNG/JPG)
- Хранение файлов в Yandex S3 или локально
- Получение информации о загруженных планах

### 🤖 AI-генерация вариантов перепланировки
- Создание запросов на перепланировку с текстовым описанием желаемых изменений
- Генерация 3-5 уникальных вариантов перепланировки с помощью ML-модели
- **Резервный режим**: При сбое основной ML-модели автоматически используется GigaChat для генерации вариантов
- Потоковая передача результатов через WebSocket в реальном времени
- Анализ соответствия нормам СНиП и ЖК РФ
- Оценка вероятности одобрения БТИ для каждого варианта
- Генерация визуализаций планов для каждого варианта

### ⭐ Избранное
- Сохранение понравившихся вариантов в избранное
- Просмотр списка избранных вариантов
- Удаление из избранного

### 📋 Заявки в БТИ
- Подача заявки на одобрение перепланировки в БТИ
- Хранение персональных данных заявителя (зашифровано)
- Отслеживание статуса заявки (pending/approved/rejected)

### 👨‍💼 Админ-панель
- Просмотр всех заявок
- Принятие решений по заявкам (одобрение/отклонение)
- Сбор обратной связи для улучшения ML-модели

## Технологии

- **Framework**: Fastify (высокопроизводительный веб-фреймворк для Node.js)
- **База данных**: PostgreSQL с Prisma ORM
- **Аутентификация**: JWT токены
- **Хранилище файлов**: Yandex S3 (с fallback на локальное хранилище)
- **AI/ML**: Основная ML-модель + GigaChat (резервный режим)
- **WebSocket**: Реал-тайм стриминг AI-ответов
- **Документация**: Swagger/OpenAPI

## Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Настройка окружения
Создайте файл `.env` на основе `.env.example` и заполните необходимые переменные (см. `ENV_SETUP.md`)

### Запуск миграций базы данных
```bash
npm run prisma:migrate
npm run prisma:generate
```

### Запуск сервера
```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Продакшн режим
npm start
```

Сервер запустится на `http://localhost:3001`

## API Документация

После запуска сервера доступна интерактивная документация Swagger:

**http://localhost:3001/docs**

## Основные эндпоинты

### Аутентификация
- `POST /auth/register` - Регистрация пользователя
- `POST /auth/login` - Вход пользователя
- `POST /admin/login` - Вход администратора

### Планы
- `POST /plans/upload` - Загрузка плана квартиры (требует авторизации)
- `GET /plans/:id` - Получение информации о плане (требует авторизации)

### AI
- `POST /ai/request` - Создание запроса на перепланировку (требует авторизации)
- `GET /ai/history` - История запросов пользователя (требует авторизации)
- `GET /ai/requests/:id` - Получение запроса с вариантами (требует авторизации)
- `GET /ai/stream/:request_id` - WebSocket стрим для получения вариантов в реальном времени

### Варианты
- `GET /variants/:id` - Получение деталей варианта (требует авторизации)
- `GET /share/:variant_id` - Публичная ссылка на вариант (без авторизации)

### Избранное
- `POST /favorites` - Добавить в избранное (требует авторизации)
- `GET /favorites` - Список избранного (требует авторизации)
- `DELETE /favorites/:variant_id` - Удалить из избранного (требует авторизации)

### Заявки
- `POST /applications` - Подать заявку в БТИ (требует авторизации)
- `GET /applications/:id` - Получить заявку (требует авторизации)

### Админ
- `GET /admin/applications` - Список всех заявок (требует админ-авторизации)
- `GET /admin/applications/:id` - Детали заявки (требует админ-авторизации)
- `POST /admin/applications/:id/decision` - Принять решение по заявке (требует админ-авторизации)

## Архитектура AI/ML

Система использует двухуровневый подход к генерации вариантов перепланировки:

1. **Основная ML-модель**: Специализированная модель для анализа планов и генерации вариантов перепланировки
2. **Резервный режим (GigaChat)**: При сбое или недоступности основной модели автоматически активируется GigaChat для обеспечения непрерывности работы системы

Все варианты генерируются на русском языке и соответствуют российским нормам строительства (СНиП 2.08.01-89, ЖК РФ).

## Структура проекта

```
backend/
├── src/
│   ├── config/          # Конфигурация (Swagger, схемы)
│   ├── controllers/     # Контроллеры API
│   ├── middleware/      # Middleware (аутентификация)
│   ├── models/          # Prisma модели
│   ├── routes/          # Маршруты API
│   ├── services/        # Бизнес-логика
│   ├── utils/           # Утилиты (JWT, файлы, GigaChat)
│   └── server.js        # Точка входа
├── prisma/
│   └── schema.prisma    # Схема базы данных
└── uploads/             # Локальное хранилище файлов (если S3 не настроен)
```

## Переменные окружения

См. `ENV_SETUP.md` для подробной информации о настройке переменных окружения.

## Лицензия

ISC

